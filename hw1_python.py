import math 

def get_data(file):
    """Simple python function to get data"""
    # read in a .txt file
    # return a list of lists of integers
    out = 2
    return out

def analyze_data(numbers, function):
    """Simple python function to operate on list of integers"""
    if function == "average":
        x = # take the mean of all numbers
    if function == "standard deviation":
        x = # take std deviation of all numbers
    if function == "covariance":
        x = # take covar
    if function == "correlation":
        x = # take covar
    return float(x)